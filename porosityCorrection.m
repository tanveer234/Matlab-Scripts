% for i = 1:length(biggs2)
%        if biggs2(i, 1)>450
%              biggs2(i, 1) = 550.34;
%        end
% end
     
%    for i = 1:length(cdp)
%         if cdp(i, 1) <= 0
%           cdp(i, 1) = mean(cdp);
%        end
%    end
%  %  
%   for i = 1:length(cnp)
%        if cnp(i, 1) <= 0
%           cnp(i, 1) = mean(cnp);
%        end
%   end

%  tp = sqrt(((dp).^2 + (np).^2)./2);
%  ep = sqrt(((cdp).^2 + (cnp).^2)./2);


% mean(ep)
% median(ep)
% min(ep)
% max(ep)
% std(ep)
% var(ep)


%swc = (bvi./ep);
% biggs = (100*((ep.^2).*(tp-bvi))./bvi).^2;
% timur = 0.136*((ep.^4.4)./(swi.^2));
% tixier = ((250*(ep.^3))./swi).^2;
% coates = ((100*(ep.^2).*(1-swi))./swi).^2;

% Coates and Dumanoir:
% c = 23 + (465*.933)-188*(.933^2);
% W = sqrt((3.75-ep) + 0.5);
% Rratio = (0.055.*Vsh.*ep.*swi./tp*7) + (((ep.*swi).^W)./((1-Vsh).^(W-1)));
% 
% dumanoir = ((c*ep.^(2*W)./((W.^4).*Rratio))).^2;

% 
%  one = one +2.5;
%  two = two -2.5;
%  three = three +3.75;
%  four = four - 3.75;







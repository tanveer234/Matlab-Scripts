%       for i = 1:length(swc)
%           if swc(i, 1) > 0.5
%              swc(i, 1) = mean(swc);
%            end
%       end
    
%    for i = 1:length(cdp)
%         if cdp(i, 1) <= 0
%           cdp(i, 1) = mean(cdp);
%        end
%    end
%  %  
%   for i = 1:length(cnp)
%        if cnp(i, 1) <= 0
%           cnp(i, 1) = mean(cnp);
%        end
%   end

%  tp = sqrt(((dp).^2 + (np).^2)./2);
%  ep = sqrt(((cdp).^2 + (cnp).^2)./2);


% mean(ep)
% median(ep)
% min(ep)
% max(ep)
% std(ep)
% var(ep)


% swc = (bvi./ep);
%biggs = (100*((ep.^2).*(tp-bvi))./bvi).^2


